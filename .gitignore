# dev environment
.devenv/
.direnv/
.devenv.flake.nix
devenv.local.nix
.pre-commit-config.yaml

# Deno-managed dependencies (regenerate with `deno install`) and frontend output
node_modules/
vendor/
frontend/dist/*

# local config & secrets
config.yaml
.env

# nix build output
result
result-*
